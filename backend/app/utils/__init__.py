# Generic, domain-agnostic helpers shared across the app — as opposed to
# app/services/, which holds domain logic (grading, feature flags, skill
# engine, etc). Empty for now; add here rather than back in a domain module
# once something is genuinely reusable across more than one domain.
