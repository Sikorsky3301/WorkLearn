import SimulationsListPanel from '../../../builder/cms/SimulationsListPanel'

/** CMS simulations list — rendered inside AdminCmsLayout / MentorCmsLayout
 * (AppRouter siblings) or as a nested AdminPortal fallback. */
export default function SimulationsPage() {
  return <SimulationsListPanel />
}
