# types

Placeholder for shared JSDoc typedefs (this is a plain JS codebase, not
TypeScript — `@typedef` blocks here can still be imported via
`/** @type {import('../types/foo').Foo} */` comments for editor
type-checking). Empty until something is genuinely shared across more than
one feature; a single feature's local shape belongs next to that feature,
not here.
