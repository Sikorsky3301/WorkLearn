# Backend API base URL. Defaults to http://localhost:3001 if unset (see
# src/shared/api/client.js) — only needed when the backend runs somewhere
# other than that default (a different port, a deployed environment, etc).
VITE_API_URL=http://localhost:3001
